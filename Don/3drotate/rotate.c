#include <stdio.h>



int main(void){
    int n;
    scanf("%d", &n);
    while(n--){
        double x,y,z;
        double rx, ry, rz;
        double theta; 
        scanf("%lf %lf %lf %lf %lf %lf %lf", &x, &y ,&z, &rx, &ry, &rz, &theta);


    }
}
