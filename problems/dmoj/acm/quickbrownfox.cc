#include <iostream>
#include <vector>
#include <bitset>
#include <string>

using namespace std;
int main()
{
  int N; 
  cin >> N;
  bitset<26> 
  while(N--){
    string str;
    getLine(str); 

    for(int i = 0; i<str.size(); i++){
      str.toLower(); 
      
    }
  }
}
